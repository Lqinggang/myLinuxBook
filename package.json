{
  "name": "myLinuxBook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "export NODE_OPTIONS=--openssl-legacy-provider && npx vuepress dev docs",
    "docs:build": "export NODE_OPTIONS=--openssl-legacy-provider && npx vuepress build docs",
    "deploy": "export NODE_OPTIONS=--openssl-legacy-provider && gh-pages -d .vuepress/dist",
    "deploy:build": "export NODE_OPTIONS=--openssl-legacy-provider && npm run docs:build && gh-pages -d .vuepress/dist"
  },
  "repository": {
    "type": "https",
    "url": "https://github.com/Lqinggang/myLinuxBook"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Lqinggang/myLinuxBook/issues"
  },
  "homepage": "https://github.com/Lqinggang/myLinuxBook#readme",
  "devDependencies": {
    "vuepress": "^1.9.10",
    "vuepress-plugin-right-anchor": "^0.5.5",
    "vuepress-plugin-side-anchor": "^1.0.6"
  },
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
